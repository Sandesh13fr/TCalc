{
  "name": "@wma/tokenizers",
  "version": "0.1.3",
  "private": true,
  "description": "Token estimation utilities including heuristic and provider-specific tokenizers.",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "lint": "eslint src/"
  },
  "dependencies": {
    "@wma/core": "workspace:*"
  },
  "devDependencies": {
    "typescript": "5.9.3"
  }
}
