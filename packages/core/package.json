{
  "name": "@wma/core",
  "version": "0.1.0",
  "private": true,
  "description": "Shared types, config, and constants for Workspace Model Advisor.",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "lint": "eslint src/"
  },
  "devDependencies": {
    "typescript": "latest"
  }
}
